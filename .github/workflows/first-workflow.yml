name: My First Workflow

on: push

jobs:
    first_job:
      runs_on: ubuntu-latest
      steps:
      - name: Welcome Message
        run: echo "Welcome"
        
      - name: List files
        run: ls