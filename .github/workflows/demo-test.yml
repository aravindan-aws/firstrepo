name: Workflow Test

on: 
    push:
        branches:
          - main
    pull_request:
      branches:
        - main
env:
  name: "Aravind"

jobs:
  First-Job:
    runs-on: ubuntu-latest
    steps:  
    - name: Welcome Message
      run: echo "Welcome" ${{ env.name }} "to My Workflow!"

  Second-Job:
    runs-on: ubuntu-latest
    strategy:
      max-parallel: 1
      matrix:
        python-version: [3.12, 3.13, 3.14, 3.10]
    steps:
    - name: Checkout
      uses: actions/checkout@v7

    - name: Read File
      run: cat README.md

    - name: Python Setup of ${{ matrix.python-version }} version
      continue-on-error: true
      uses: actions/setup-python@v7
      with:
        python-version: $ {{ matrix.python-version }}

    - name: Version Check
      if: github.ref == 'refs/heads/main' && success()
      run: python --version

  Third-Job:
    runs-on: ubuntu-latest
    needs: [Second-Job, First-Job]
    if: github.ref == 'refs/heads/main' && success()
    steps:
    - name: Checkout
      uses: actions/checkout@v7

    - name: Read File if it is in Respective Branch
      run: cat README.md